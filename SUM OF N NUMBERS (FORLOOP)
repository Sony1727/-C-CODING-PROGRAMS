#include <stdio.h>

int main()
{
    int number,i,sum;
    printf("enter the numbers");
    scanf("%d",&number);
    for(i=1;i<number;i++)
    {
        sum=sum +i;
    }
    printf("%d",sum);
    
    
    
    return 0;
}
