#include <stdio.h>
int sum();
int main()
{
    int f;
   f=sum();
   printf("result is %d",f);
    return 0;
}

int sum(){
    int a,b,sum;
    printf("enter two numbers :");
    scanf("%d%d",&a,&b);
    sum=a+b;
    return sum;
    
}
