
#include <stdio.h>

int main()
{
    int number,i,num,n,mult;
    printf("Enter the number");
    scanf("%d",&num);
    for(i=1;i<=30;i++)
    {
         printf ("\n %d * %d = %d", num, i, (num*i));  
    }
    return 0;
}
